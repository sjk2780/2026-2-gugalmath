# CLAUDE.md — 프로젝트 인수인계 문서

> 이 파일은 Claude Code가 프로젝트를 이어받을 때 자동으로 읽는 문서입니다.
> 지금까지의 맥락, 결정사항, 반드시 지킬 규칙이 정리되어 있습니다.

\---

## 1\. 프로젝트 개요

중학교 2학년 수학 교사(구갈중학교)를 위한 **태블릿 기반 수업 활동 도구**.
학생이 태블릿으로 문제를 풀어 제출하면, 교사가 한눈에 확인하는 파이프라인.

**목표**: 매 차시 교사가 코드를 건드리지 않고 문제만 바꿔서 재사용하는 자동화 시스템.

**호스팅**: GitHub Pages (정적 HTML). 교사가 직접 배포.
**백엔드**: Firebase Realtime Database (databaseURL: `https://gugal-math-default-rtdb.firebaseio.com/`)
**폰트/수식**: Pretendard (본문), KaTeX (수식). CDN 로드.
**접속**: 학생은 QR/URL, 교사는 URL 뒤 `?teacher=true`.

\---

## 2\. 현재 파일 구성

|파일|용도|상태|
|-|-|-|
|`수업활동\_자동.html`|**메인.** 앱 안에서 교사가 문제를 출제하는 자동 버전|최신, 권장|
|`수업활동\_틀.html`|문제를 코드(맨 위 CONFIG/PROBLEMS)에서 바꾸는 수동 틀|구버전(백업용)|
|`교점찾기\_통합.html`|4.7단원(연립방정식과 그래프) 전용. 좌표평면 그래프 엔진 포함|완성(단원 특화)|
|`자동출제\_사용법.md`|교사용 한글 사용 설명서|—|

앞으로는 **`수업활동\_자동.html`** 을 기본으로 발전시킬 것.

\---

## 3\. 데이터 구조 (Firebase)

`수업활동\_자동.html` 기준. 고정 방 이름 `ROOM = 'gugal\_class'`.

```
live/gugal\_class/
  meta        → { title: "수업 제목" }
  problems    → \[ {label, text, math, image(base64)}, ... ]   ← 교사가 출제한 현재 문제
  subs/
    <pushKey> → { class, name, pIdx, label, img(base64), time }  ← 학생 제출
```

* 문제 교체: `problems`를 `set()`으로 통째 교체. **제출(`subs`)은 안 지움** (교사 요구사항).
* 제출: 매번 `push()`로 고유 키 생성 → 절대 안 겹침.

교점찾기(4.7)는 별도 경로 `math\_results/crossing` 사용.

\---

## 4\. ★★★ 반드시 지킬 규칙 (하드윈 교훈) ★★★

### (1) Firebase forEach — 콜백에서 truthy 반환 금지

Firebase의 `snapshot.forEach(cb)`는 **cb가 truthy를 반환하면 순회를 즉시 중단**한다.
`arr.push()`는 새 길이(1 이상 = truthy)를 반환하므로, 화살표 함수 암묵 반환으로 쓰면 첫 항목에서 멈춘다.

```js
// ❌ 절대 금지 (첫 개만 읽힘 — 실제로 이 버그로 며칠 헤맴)
snap.forEach(c => window.allData.push({id:c.key, ...c.val()}));

// ✅ 반드시 블록으로 감싸 반환값 없앰
snap.forEach(c => { window.allData.push({id:c.key, ...c.val()}); });
```

### (2) 카드 onclick에 이미지 데이터(base64) 넣지 말 것

base64가 크면(스케치 이미지) HTML 속성이 깨져 뒤 카드가 안 그려진다.
→ `recCache{}`에 데이터 저장하고 onclick엔 **id만** 넘긴다.

```js
img.onclick = () => openInspect(id);  // recCache\[id]에서 조회
```

### (3) 제출 저장은 push(), 절대 고정 키 금지

`반\_이름\_문제` 같은 고정 키를 쓰면 재제출/동명이인이 덮어써진다. 반드시 `ref.push()`.

### (4) 배포 후 강력 새로고침 안내

GitHub Pages는 커밋 후 빌드에 1\~2분. 브라우저 캐시 때문에 옛 버전이 보인다.
사용자에게 항상 `Ctrl+Shift+R`(모바일은 캐시 삭제/시크릿) 안내.

### (5) 입력값 이스케이프

출제 화면에서 사용자 입력을 HTML 속성/본문에 넣을 때 `escAttr`/`escHtml` 사용.
특히 수식의 백슬래시 처리 주의(`\\` → 저장/표시 경로에 따라 개수 관리).

\---

## 5\. 확정된 설계 결정 (되돌리지 말 것)

* **자동 채점 없음.** 정답/오답 판정하지 않고 무조건 제출. 교사가 눈으로 확인.
* **답칸 없음.** 학생은 빈 노트(스케치북)에 손으로 풀이만 제출. (도형/서술 단원 대응)
* **문제 교체 시 제출 유지.** 새 출제해도 이전 제출 안 지움.
* **그림은 base64로 Firebase 저장.** 파일 선택 → FileReader로 dataURL 변환. (imgur 등 외부 안 씀)
* **수식은 KaTeX.** 변수는 이탤릭 세리프로 교과서 느낌. 입력 시 실시간 미리보기 제공.
* **디자인**: Pretendard 기반 미니멀. 색상 토큰: `--ink:#1A1A1A --sub:#6B7280 --line:#E5E7EB --ok:#059669`. 과한 장식 지양.
* **미션의 "그릴 직선" 힌트 제거**(학생이 스스로 발견). 문제 질문은 교과서 문구 그대로.

\---

## 6\. 알려진 한계 / 개선 여지 (다음 작업 후보)

* **이미지 base64 → Firebase Storage 이전**: 그림을 많이/크게 쓰면 RTDB가 무거워짐. 현재 1MB 경고만 있음. 장기적으로 Storage나 외부 CDN 권장.
* **출제 화면 접근 보호**: 지금 `?teacher=true`만 알면 누구나 출제 가능. 간단한 PIN/비밀번호 잠금 고려.
* **차시 보관**: 현재는 "현재 문제"만 유지(덮어씀). 차시별 히스토리 저장 기능 없음(교사가 불필요하다고 함, 필요 시 추가).
* **문제 유형 확장**: 현재 스케치북(범용) + 좌표평면(4.7 전용). 도형 작도, 객관식 등 유형 추가 가능.
* **반응형**: 모바일 대응돼 있으나 태블릿 가로/세로 추가 QA 권장.

\---

## 7\. 남은 제작 단원 (교사 계획)

중2 2학기: **5.삼각형의 성질 / 6.사각형의 성질 / 7.도형의 닮음과 피타고라스 / 8.경우의 수와 확률**
→ 전부 그래프 아님(도형·계산·서술). **스케치북 범용 틀(`수업활동\_자동.html`)로 커버.**
→ 도형 단원은 문제에 그림(이미지) 필수.

\---

## 8\. 테스트 방법 (이 환경에서 검증할 때)

Playwright로 검증. Firebase는 mock 주입. **주의: mock의 forEach도 실제처럼 "truthy 반환 시 중단"을 재현해야** 위 (1)번 버그를 잡을 수 있음(안 그러면 통과해버림).

핵심 시나리오:

1. 교사 출제 → 학생이 실시간으로 문제 수신하는지
2. 학생 여러 명/여러 문제 제출 → 교사 대시보드에 **전부** 뜨는지 (forEach 버그 회귀 확인)
3. 같은 이름·같은 문제 재제출 → 덮어쓰지 않고 쌓이는지
4. 스케치 이미지가 큰 제출 여러 개 → 카드가 다 렌더되는지 (onclick base64 버그 회귀 확인)

\---

## 9\. 교사 커뮤니케이션 스타일

* 직접적이고 구체적인 결과물 선호. 추상적 원칙보다 바로 쓸 수 있는 것.
* 코딩 최소화 원함. 화면 조작으로 끝나는 걸 선호.
* 버그 발견 시 정확한 증상 관찰을 잘함(예: "서버 원시 3개인데 forEach 1개"). 진단 도구를 만들어 함께 원인 규명하는 방식이 잘 통함.
* 한국어로 소통. 존중하며 간결하게.

\---

## 10\. 다음 세션 시작 시 추천 행동

1. 이 문서와 `수업활동\_자동.html` 읽기.
2. 교사에게 어떤 단원/작업을 할지 확인.
3. 파일 수정 후 반드시 위 §8 시나리오로 회귀 테스트.
4. 변경사항은 Git 커밋. 배포 후 강력 새로고침 안내.
5. 이 `CLAUDE.md`도 새 결정/교훈이 생기면 업데이트.


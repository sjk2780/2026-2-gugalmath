# 수업 활동 (자동 출제 버전) 사용법

이 버전은 **코드를 전혀 고치지 않아요.** 화면에서 문제를 입력하면 학생에게 바로 나갑니다.
파일은 딱 하나(`수업활동\_자동.html`)만 GitHub에 올려두고, 매 차시 계속 씁니다.

\---

## 처음 한 번만: 배포

1. `수업활동\_자동.html` 을 GitHub에 올립니다 (예: `class.html`)
2. 주소가 생깁니다

   * **학생용**: `https://내주소.github.io/class.html`
   * **교사용**: 뒤에 `?teacher=true` → `https://내주소.github.io/class.html?teacher=true`

이 두 주소는 앞으로 계속 같습니다. 매 차시 새로 안 만들어요.

\---

## 매 차시 하는 일 (아주 간단)

### 1\. 교사용 주소로 접속 → "출제하기" 탭

* **수업 제목** 입력 (예: 6단원 사각형의 성질)
* **문제 설명** 입력
* 필요하면 **수식**, **도형 그림**(파일 선택) 추가
* 문제가 더 필요하면 **"+ 문제 추가"**
* 다 됐으면 **"이 문제로 수업 시작"** 클릭

→ 이 순간 학생 화면에 새 문제가 나타납니다.

### 2\. 학생들 접속

* 학생용 주소(또는 교사 화면의 QR)로 접속
* 문제 골라 노트에 손으로 풀이 → 반·이름 넣고 제출

### 3\. "제출 현황" 탭에서 확인

* 문제별로 제출된 풀이가 보임
* 카드를 누르면 크게 보임
* 필요 없는 건 삭제

\---

## 좋은 점

* **파일을 매번 안 만들어요.** 한 파일로 계속.
* **코드를 안 건드려요.** 화면에서 입력만.
* **폰·태블릿에서도 출제 가능** (교사용 주소로 접속하면 됨).
* **도형 그림**을 파일 선택으로 바로 올림 (imgur 같은 데 안 올려도 됨).

\---

## 알아둘 점

### 새 문제를 내면

* 학생 화면의 **문제만 바뀝니다.**
* **이전에 제출된 풀이는 지워지지 않아요.** (안전)
* 이전 제출을 정리하고 싶으면 "제출 현황"에서 삭제 버튼으로 지우면 됩니다.

### 수식 입력 (선택)

수식이 필요하면 KaTeX 문법으로 씁니다. 입력하면 바로 아래 미리보기가 떠요.
자주 쓰는 것:

* `\\angle A` → ∠A
* `70^\\circ` → 70°
* `\\overline{AB}` → 선분 AB
* `\\triangle ABC` → 삼각형 ABC
* `\\frac{1}{2}` → 분수
* `x^2` → x²
* `\\sqrt{2}` → √2
* `a^2 + b^2 = c^2` → 피타고라스

(이 화면에서는 백슬래시 `\\` 를 **한 개**만 쓰면 됩니다. 미리보기로 확인하세요.)

### 도형 그림

* "파일 선택"으로 폰/컴퓨터의 그림을 고르면 자동 업로드
* 그림이 너무 크면(1MB 넘으면) 경고가 떠요 → 더 작은 그림 사용
* 교과서 사진을 찍거나 캡처해서 올리면 됩니다

### 배포 후 강력 새로고침

파일을 처음 올리거나 업데이트한 뒤에는:

* 컴퓨터: `Ctrl + Shift + R`
* 태블릿: 캐시 지우고 새로고침 또는 시크릿 창

\---

## 수업 전 점검 (권장)

* 교사용으로 문제 하나 출제
* 다른 기기(폰)로 학생 접속해서 문제가 보이는지, 제출되는지 확인
* 교사 "제출 현황"에 뜨는지 확인

이 3가지만 되면 수업 준비 끝입니다.

\---

## 문제가 생기면

* **학생 화면에 문제가 안 떠요** → 교사가 "이 문제로 수업 시작"을 눌렀는지 확인. 학생은 강력 새로고침.
* **제출이 교사 화면에 안 떠요** → "새로고침" 버튼 클릭. 그래도 안 되면 강력 새로고침.
* **수식이 이상해요** → 미리보기를 보며 KaTeX 문법 확인.
* **그림이 안 올라가요** → 그림 용량을 줄여보세요(1MB 이하).


# CLAUDE.md — 프로젝트 인수인계 문서

> 이 파일은 Claude Code가 프로젝트를 이어받을 때 자동으로 읽는 문서입니다.
> 지금까지의 맥락, 결정사항, 반드시 지킬 규칙이 정리되어 있습니다.

\---

## 1\. 프로젝트 개요

중학교 2학년 수학 교사(구갈중학교)를 위한 **태블릿 기반 수업 활동 도구**.
학생이 태블릿으로 문제를 풀어 제출하면, 교사가 한눈에 확인하는 파이프라인.

**목표**: 매 차시 교사가 코드를 건드리지 않고 문제만 바꿔서 재사용하는 자동화 시스템.

> ⚠️ **2026-08-26 예외**: 5단원은 교사 요청으로 이 목표와 다르게, **소단원(5.1~5.4)마다 별도 html 파일에 문제를 코드로 하드코딩**하는 방식으로 만듦. 아래 §2, §5 참고. 6·7·8단원도 같은 방식을 원하는지는 매번 교사에게 확인할 것(자동 방식으로 되돌리고 싶을 수도 있음).

**호스팅**: GitHub Pages (정적 HTML). 교사가 직접 배포.
**백엔드**: Firebase Realtime Database (databaseURL: `https://gugal-math-default-rtdb.firebaseio.com/`)
**폰트/수식**: Pretendard (본문), KaTeX (수식). CDN 로드.
**접속**: 학생은 QR/URL, 교사는 URL 뒤 `?teacher=true`.

\---

## 2\. 현재 파일 구성

|파일|용도|상태|
|-|-|-|
|`수업활동\_자동.html`|**메인.** 앱 안에서 교사가 문제를 출제하는 자동 버전|최신, 권장|
|`수업활동\_틀.html`|문제를 코드(맨 위 CONFIG/PROBLEMS)에서 바꾸는 수동 틀|구버전(5.1~5.4의 1차 원본이었음)|
|**`수업활동\_탐구틀.html`**|**5.1~5.4의 현재 베이스 템플릿.** 탐구 모드 엔진 + 그림 위 필기 + 되돌리기 포함. `<!-- BUILD:CONFIG\_START -->`\~`END` 사이만 빌드 스크립트가 교체|**2026-08-27 신규, 앞으로 이걸 씀**|
|`5.1.html` ~ `5.4.html`|5단원 소단원별 파일. **`수업활동\_탐구틀.html`에서 생성.** 학생 탭 = \[탐구하기]\[문제 풀기], 교사는 제출 현황만|2026-08-27 재생성|
|`자동출제\_사용법.md`|교사용 한글 사용 설명서 (`수업활동\_자동.html` 기준, 5.1~5.4엔 해당 없음)|—|
|`중학\_수학2\_교과서.pdf` / `중학\_수학2\_지도서.pdf`|출제 시 참고용 원본 교과서/지도서|—|

일반 단원은 **`수업활동\_자동.html`** 을 기본으로, 5단원처럼 "소단원별 고정 문제" 요청이 오면 `수업활동\_틀.html`을 베이스로 §5의 방식대로 새 파일을 만들 것.

> ⚠️ **2026-08-26 기준**: 이전 문서에 언급됐던 `교점찾기\_통합.html`(4.7단원 전용)이 이 폴더에 실제로 없음(다른 위치에 있거나 유실된 것으로 보임). 필요하면 교사에게 위치를 확인할 것.

\---

## 3\. 데이터 구조 (Firebase)

`수업활동\_자동.html` 기준. 고정 방 이름 `ROOM = 'gugal\_class'`.

```
live/gugal\_class/
  meta        → { title: "수업 제목" }
  problems    → \[ {label, text, math, image(base64)}, ... ]   ← 교사가 출제한 현재 문제
  subs/
    <pushKey> → { class, name, pIdx, label, img(base64), time }  ← 학생 제출
```

* 문제 교체: `problems`를 `set()`으로 통째 교체. **제출(`subs`)은 안 지움** (교사 요구사항).
* 제출: 매번 `push()`로 고유 키 생성 → 절대 안 겹침.

교점찾기(4.7)는 별도 경로 `math\_results/crossing` 사용.

`5.1.html`~`5.4.html`(틀.html 기반)은 다른 DB 루트 사용: `classroom/<CONFIG.folder>` (예: `classroom/unit5_1`). 파일마다 `folder` 값이 달라서 제출이 서로 안 섞인다.

\---

## 4\. ★★★ 반드시 지킬 규칙 (하드윈 교훈) ★★★

### (1) Firebase forEach — 콜백에서 truthy 반환 금지

Firebase의 `snapshot.forEach(cb)`는 **cb가 truthy를 반환하면 순회를 즉시 중단**한다.
`arr.push()`는 새 길이(1 이상 = truthy)를 반환하므로, 화살표 함수 암묵 반환으로 쓰면 첫 항목에서 멈춘다.

```js
// ❌ 절대 금지 (첫 개만 읽힘 — 실제로 이 버그로 며칠 헤맴)
snap.forEach(c => window.allData.push({id:c.key, ...c.val()}));

// ✅ 반드시 블록으로 감싸 반환값 없앰
snap.forEach(c => { window.allData.push({id:c.key, ...c.val()}); });
```

### (2) 카드 onclick에 이미지 데이터(base64) 넣지 말 것

base64가 크면(스케치 이미지) HTML 속성이 깨져 뒤 카드가 안 그려진다.
→ `recCache{}`에 데이터 저장하고 onclick엔 **id만** 넘긴다.

```js
img.onclick = () => openInspect(id);  // recCache\[id]에서 조회
```

### (3) 제출 저장은 push(), 절대 고정 키 금지

`반\_이름\_문제` 같은 고정 키를 쓰면 재제출/동명이인이 덮어써진다. 반드시 `ref.push()`.

### (4) 배포 후 강력 새로고침 안내

GitHub Pages는 커밋 후 빌드에 1\~2분. 브라우저 캐시 때문에 옛 버전이 보인다.
사용자에게 항상 `Ctrl+Shift+R`(모바일은 캐시 삭제/시크릿) 안내.

### (5) 입력값 이스케이프

출제 화면에서 사용자 입력을 HTML 속성/본문에 넣을 때 `escAttr`/`escHtml` 사용.
특히 수식의 백슬래시 처리 주의(`\\` → 저장/표시 경로에 따라 개수 관리).

### (6) Windows에서 로컬 브라우저 테스트 시 한글 경로 주의

이 프로젝트 폴더 경로(`2026-2수업`)에 한글이 들어 있어, `file://`로 그냥 열면 Browser 도구가 "static snapshot"(JS 미실행)으로만 렌더링한다.
**반드시 로컬 정적 서버를 띄워서 `http://localhost:PORT/파일명`으로 접속**해야 JS가 실제로 동작한다.
이 환경엔 Python/Node가 없어서 PowerShell `System.Net.HttpListener`로 서버를 대신 띄웠는데, 이때 스크립트(.ps1) **파일 안에 한글 리터럴을 직접 쓰면 Windows PowerShell 5.1이 기본 ANSI 코드페이지로 읽어 깨진다.** 폴더 경로는 `-Root` 같은 **커맨드라인 인자로 전달**하고(인자는 유니코드로 정상 마샬링됨), 요청 경로는 `[System.Uri]::UnescapeDataString`으로 런타임에 디코딩하면 문제없다. 스크립트 소스 안에는 한글 문자열을 아예 넣지 않는 게 안전.
또한 이 세션에서는 Browser 도구의 `screenshot`이 항상 실패했다(pane 비표시) — 대신 `javascript_tool`로 `startDraw/moveDraw/endDraw` 같은 함수를 직접 호출하고 `strokes` 배열 등 내부 상태를 `JSON.stringify`로 읽어 검증하는 방식이 잘 통했다.

\---

## 5\. 확정된 설계 결정 (되돌리지 말 것)

* **자동 채점 없음.** 정답/오답 판정하지 않고 무조건 제출. 교사가 눈으로 확인.
* **답칸 없음.** 학생은 빈 노트(스케치북)에 손으로 풀이만 제출. (도형/서술 단원 대응)
* **문제 교체 시 제출 유지.** 새 출제해도 이전 제출 안 지움.
* **그림은 base64로 Firebase 저장.** 파일 선택 → FileReader로 dataURL 변환. (imgur 등 외부 안 씀)
* **수식은 KaTeX.** 변수는 이탤릭 세리프로 교과서 느낌. 입력 시 실시간 미리보기 제공.
* **디자인**: Pretendard 기반 미니멀. 색상 토큰: `--ink:#1A1A1A --sub:#6B7280 --line:#E5E7EB --ok:#059669`. 과한 장식 지양.
* **미션의 "그릴 직선" 힌트 제거**(학생이 스스로 발견). 문제 질문은 교과서 문구 그대로.
* **교과서 문제 인용 범위: "문제 N"이라고 명시된 것만.** 예제·함께해요·생각해요·생각넓히기·해결해요·수학실험실·수학돋보기·단원마무리·프로젝트에 실린 문제는 절대 가져오지 않는다(교사가 명시적으로 지시함, 저작권/난이도 성격이 다른 것으로 보임). 새 단원 작업 시 항상 이 기준으로 필터링할 것.
* **소단원별 고정 파일 방식(5단원)**: 문제 그림은 imgur 같은 외부 호스팅 대신, 교과서 PDF에서 크롭한 PNG를 **base64로 코드에 직접 내장**한다(§11 파이프라인 참고). `수업활동\_틀.html`의 `image: "URL"` 방식(예시 주석)은 실제로는 안 씀.

\---

## 6\. 알려진 한계 / 개선 여지 (다음 작업 후보)

* **이미지 base64 → Firebase Storage 이전**: 그림을 많이/크게 쓰면 RTDB가 무거워짐. 현재 1MB 경고만 있음. 장기적으로 Storage나 외부 CDN 권장.
* **출제 화면 접근 보호**: 지금 `?teacher=true`만 알면 누구나 출제 가능. 간단한 PIN/비밀번호 잠금 고려.
* **차시 보관**: 현재는 "현재 문제"만 유지(덮어씀). 차시별 히스토리 저장 기능 없음(교사가 불필요하다고 함, 필요 시 추가).
* **문제 유형 확장**: 현재 스케치북(자유 필기 + 직선 도구) + 좌표평면(4.7 전용, 별도 파일). 원(컴퍼스), 점 스냅(격자) 등은 아직 없음 — 필요시 추가.
* **반응형**: 모바일 대응돼 있으나 태블릿 가로/세로 추가 QA 권장.

\---

## 7\. 남은 제작 단원 (교사 계획)

중2 2학기: **5.삼각형의 성질 / 6.사각형의 성질 / 7.도형의 닮음과 피타고라스 / 8.경우의 수와 확률**
→ 전부 그래프 아님(도형·계산·서술). **스케치북 범용 틀로 커버** — 단, 5단원은 교사 요청으로 `수업활동\_자동.html`(출제화면 방식)이 아니라 `수업활동\_틀.html` 기반 **소단원별 개별 파일**(`5.1.html`~`5.4.html`)로 만듦.
→ 도형 단원은 문제에 그림(이미지) 필수.
→ 6·7·8단원 시작 전에 **"자동 출제 방식으로 할지, 5단원처럼 소단원별 고정 파일로 할지"** 반드시 먼저 확인. 후자를 원하면 §11의 빌드 파이프라인(교과서 크롭 → base64 → 틀.html 기반 생성)을 그대로 재사용.

\---

## 8\. 테스트 방법 (이 환경에서 검증할 때)

Playwright로 검증. Firebase는 mock 주입. **주의: mock의 forEach도 실제처럼 "truthy 반환 시 중단"을 재현해야** 위 (1)번 버그를 잡을 수 있음(안 그러면 통과해버림).

핵심 시나리오:

1. 교사 출제 → 학생이 실시간으로 문제 수신하는지
2. 학생 여러 명/여러 문제 제출 → 교사 대시보드에 **전부** 뜨는지 (forEach 버그 회귀 확인)
3. 같은 이름·같은 문제 재제출 → 덮어쓰지 않고 쌓이는지
4. 스케치 이미지가 큰 제출 여러 개 → 카드가 다 렌더되는지 (onclick base64 버그 회귀 확인)

\---

## 9\. 교사 커뮤니케이션 스타일

* 직접적이고 구체적인 결과물 선호. 추상적 원칙보다 바로 쓸 수 있는 것.
* 코딩 최소화 원함. 화면 조작으로 끝나는 걸 선호.
* 버그 발견 시 정확한 증상 관찰을 잘함(예: "서버 원시 3개인데 forEach 1개"). 진단 도구를 만들어 함께 원인 규명하는 방식이 잘 통함.
* 한국어로 소통. 존중하며 간결하게.

\---

## 10\. 다음 세션 시작 시 추천 행동

1. 이 문서와 `수업활동\_자동.html` 읽기.
2. 교사에게 어떤 단원/작업을 할지 확인.
3. 파일 수정 후 반드시 위 §8 시나리오로 회귀 테스트.
4. 변경사항은 Git 커밋. 배포 후 강력 새로고침 안내.
5. 이 `CLAUDE.md`도 새 결정/교훈이 생기면 업데이트.

\---

## 11\. 변경 이력

* **2026-08-27: 탐구 모드 내장 (`수업활동\_탐구틀.html` 신설)** — 교사 요청의 핵심 배경: **학생들이 알지오매스 사이트를 왔다 갔다 하는 것을 어려워한다**(사이트가 자꾸 꺼지고, 매 시간 "어떻게 하냐" 질문이 나옴). 그래서 동적 기하 탐구를 외부 사이트에 맡기지 않고 **앱 안에 직접 구현**함. 학생은 한 화면에서 \[탐구하기]→\[문제 풀기]로 이동.
  - 소단원별 탐구 모드(`CONFIG.explore`): `isosceles`(5.1) / `congruence`(5.2) / `circumcenter`(5.3) / `incenter`(5.4).
    - **5.1**: `AB=AC 고정` 토글 켜고 꼭짓점을 끌면 두 밑각이 **항상 같게** 유지됨(실시간 각도 표시) → 성질을 스스로 발견. 꼭지각 이등분선 토글 시 BD=DC, ∠ADB=90° 동시 표시.
    - **5.2**: 합동인 두 직각삼각형을 **드래그·회전(15°)·뒤집기로 직접 포개보기**(교과서 152쪽 서술 방식 그대로). 세 꼭짓점이 tol 14px 안에 들어오면 "포개졌습니다" 안내. 프리셋 2개(RHA/RHS).
    - **5.3**: 세 변의 수직이등분선을 **하나씩** 토글 → 2개 이상이면 O와 OA=OB=OC 표시, 3개면 "한 점에서 만납니다" 안내. 삼각형 종류(예각/직각/둔각)와 **점 O의 위치**(내부/빗변의 중점/외부)를 실시간 표시.
    - **5.4**: 세 내각의 이등분선 토글 → I, ID=IE=IF, 내접원.
  - **★★ 교훈(가장 중요 — 탐구 도구 만들 때 항상 확인할 것)**: **표시 정밀도가 항목마다 다르면 배우려는 성질과 정반대되는 화면이 나온다.** 5.1에서 길이는 `0.1cm`(≈9px) 단위로 반올림 표시되는데 각도는 `0.1°` 단위라 훨씬 예민하다. 그래서 두 변이 4px만 달라도 화면엔 **"AB 6.4cm, AC 6.4cm"(같음)인데 "∠B 60.5°, ∠C 59.8°"(다름)** 로 떠서, 학생이 *"선생님, 변의 길이는 같은데 두 밑각의 크기가 달라요"* 라고 말하게 된다(교사가 실제로 이 위험을 먼저 지적함).
    - 해결: **거의 이등변이면 정확히 이등변으로 스냅**(`exSnapIso`, 허용오차 = 표시 단위 0.1cm). 끄는 점이 C면 A에서의 거리를, B면 반대로, A면 BC의 수직이등분선 위로 투영. → "같아 보이면 실제로도 같다"가 보장됨.
    - 검증: 무작위 드래그 400회 + 애매구간 촘촘히 29회에서 **모순 0건**, 동시에 390/400은 여전히 "이등변 아님"으로 표시(스냅이 과하지 않음)를 확인. 새 탐구 모드를 만들면 **반드시 이 "표시 일관성" 테스트를 돌릴 것.**
  - **★ 교훈(중요)**: 꼭짓점을 끌어 삼각형이 **거의 일직선**이 되면 외심이 화면 밖 수천 px로 날아가 화면이 깨진다(실측 반지름 4470px). `tooFlat()`(최대각 165° 초과)로 **드래그를 되돌리는 가드**를 넣어야 함. 학생들은 반드시 이 상태를 만든다.
  - **★ 교훈**: 길이를 고정 px→cm 상수로 환산하면 화면 크기에 따라 "13.1 cm"처럼 교과서와 동떨어진 값이 나온다. **캔버스 가로를 항상 12cm로 간주**(`CANVAS_CM`)해서 기기와 무관하게 5\~7cm대가 나오도록 함.
  - **★ 교훈**: `window.onload` 시점엔 `aspect-ratio` 레이아웃이 아직 안 잡혀 `clientWidth/Height`가 0 → 캔버스가 300x150인 채로 남고 삼각형이 안 그려진다. **`ResizeObserver`로 두 캔버스를 관찰**하고 rAF·setTimeout 재호출까지 걸어야 안정적.
  - 문제 풀기 화면: **문제 그림을 캔버스 배경으로 그려서 도형 위에 바로 필기**(등변·등각 표시, 보조선). 기존처럼 그림을 캔버스 위쪽에 따로 두지 않음. `되돌리기`(strokes.pop) 버튼 추가.
  - **★ 교훈: 배경 그림을 깔면 "흰색 덧칠" 지우개를 쓰면 안 된다.** 기존 지우개는 `color:'#ffffff'`로 흰 선을 긋는 방식이라, 그림을 배경으로 옮긴 뒤엔 **지우개가 문제 그림까지 지워 버렸다**(교사가 발견). 해결: **필기 전용 오프스크린 레이어(`inkCv`)** 를 만들어 획을 거기에 그리고, 지우개 획은 `globalCompositeOperation='destination-out'`으로 그 레이어에서만 지운 뒤, 배경 위에 `drawImage`로 합성한다. 획에는 `erase:true` 플래그를 둔다(색으로 판별하지 말 것). 검증: 그림 한복판을 지우개로 문질러도 그림 픽셀 9360→9360 불변, 필기는 2292→0으로 정상 삭제, 되돌리기로 2292 복구, 제출 PNG엔 그림+필기 모두 포함.
  - 지우개만 쓰고 제출하는 것을 막기 위해 제출 조건은 `strokes.some(s=>!s.erase)`.
  - 탐구 화면도 제출 가능(`pIdx = -1`, label `'탐구'`). 대시보드는 `탐구` 섹션을 맨 앞에 표시.
  - 교사가 **발표 모드(제출물 나란히 비교)는 불필요하다고 명시함** — "그건 내가 충분히 극복 가능". 추가하지 말 것.


* **2026-08-26**: 스케치북에 **직선(자) 도구** 추가 — `sketch-tools`에 "직선" 버튼, `lineMode` 상태로 자유 필기와 배타적 토글(지우개와도 배타적). 드래그 시작점 고정 + 끝점만 계속 갱신하는 방식(`startDraw`/`moveDraw` 수정)이라 `draw()` 렌더링 로직은 그대로 재사용됨(2점짜리 stroke는 자연히 직선). 5단원(삼각형의 성질) 자유 작도 대응 목적. 원(컴퍼스)/점 스냅은 아직 없음 — 교사가 필요하다고 하면 추가.
* PDF 렌더링 도구(poppler `pdftoppm`)가 이 컴퓨터에 없어서 `winget install oschwartz10612.Poppler`로 설치함(향후 세션에서도 PATH에 없으면 재설치 필요할 수 있음 — 설치 위치는 `%LOCALAPPDATA%\Microsoft\WinGet\Packages\oschwartz10612.Poppler_*\poppler-*\Library\bin`). 참고로 이 poppler 빌드로 `pdftotext`를 그냥 쓰면 `Unknown character collection 'Adobe-Korea1'` 에러로 한글이 다 깨진다 — 텍스트 추출 대신 `pdftoppm`으로 페이지를 이미지로 렌더링해서 비전으로 읽는 방식을 씀.

* **2026-08-26: 5.1~5.4.html 생성 파이프라인** (6·7·8단원에서 소단원별 고정 파일을 또 만들게 되면 그대로 재사용):
  1. `pdftoppm -r 220`으로 필요한 교과서 페이지만 고해상도 PNG로 렌더링.
  2. 각 "문제 N"의 도형 부분만 PowerShell(`System.Drawing.Bitmap`)으로 정밀 크롭(좌표는 저해상도 전체 페이지를 먼저 눈으로 보고 추정 → 크롭 → 결과 확인 → 필요시 좌표 보정, 1~2회 반복하면 됨).
  3. 문제 텍스트/라벨/이미지파일명을 **별도 UTF-8 JSON 파일**(`units.json`)로 정리.
  4. PowerShell 빌드 스크립트가 `수업활동\_틀.html`을 읽어 (a) 직선 도구 코드를 삽입하고 (b) `units.json`을 읽어 각 이미지를 base64로 인코딩해 `CONFIG`/`PROBLEMS` 블록을 생성 → 소단원별 html로 저장.
  - **★ 중요 교훈 1**: PowerShell 5.1은 `.ps1` 스크립트 소스 안에 한글 리터럴이 있으면 인코딩이 깨진다(§4-(6) 참고). 빌드 스크립트에는 **순수 ASCII 앵커 문자열**(함수명, CSS 셀렉터 등)만 쓰고, 한글 데이터는 전부 별도 JSON 파일에서 런타임에 읽어와야 안전하다.
  - **★ 중요 교훈 2**: `수업활동\_틀.html`은 안내 주석 안에 `<script>`라는 단어가 문자 그대로 들어있다(`"이 <script> 안의 내용만..."`). `IndexOf("<script>")`로 진짜 스크립트 태그를 찾으면 이 주석 속 문구가 먼저 걸려서, 생성된 파일에서 CONFIG가 주석에 먹혀버려 실행이 안 되는 버그가 남. **`const CONFIG = {`를 먼저 찾고 그 앞에서 `LastIndexOf("<script>")`로 진짜 태그를 역탐색**해야 한다.
  - 문제 그림 이미지는 `image: 'data:image/png;base64,...'` 형태로 코드에 직접 내장(외부 URL 안 씀, §5 참고).

